Legacy NeoPulse frontend
=========================

This folder is a backup copy of the older "NeoPulse" frontend.

Location: frontend/legacy_neopulse

Purpose:
- Preserve the alternate implementation and experimental components (Three.js / D3 visualizations).
- Use for reference when consolidating unique components into `frontend/src/components`.

Notes:
- This copy is archival — it may not be a complete Vite app (some entry files omitted).
- Do not delete until canonical `frontend/src` has been fully merged and tested.
